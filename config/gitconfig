[user]
    name = {{ .git.user | quote }}
    email = {{ .git.email | quote }}
    signingkey = {{ .git.email | quote }}

[commit]
    gpgSign = true

[gpg]
    format = ssh

[status]
    submodulesummary = true
